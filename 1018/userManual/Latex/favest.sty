\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{sparsemicros}[2018/09/16 Custom Package]

\RequirePackage{amsfonts}
\RequirePackage{amsfonts}
\RequirePackage{mathrsfs}
\RequirePackage{amssymb}
\RequirePackage{amsmath}
\RequirePackage{amsthm}

%%%%%%

%%% 'micros for sphere and needlet' option %%%
\newcommand{\bullety}{ % bullet dot in yellow
\textcolor{yellow!75!red}{$\bullet$}\hspace{-0.3mm}
}

\newcommand{\bulletw}{ % bullet dot in yellow
\textcolor{white}{$\bullet$}\hspace{-0.3mm}
}

\newcommand{\alertred}[1]{ % citing in a slide
    \textcolor{red!50!green}{#1}
    }

\newcommand{\alertyellow}[1]{ % citing in a slide
    \textcolor{yellow!75!red}{#1}
    }

\newcommand{\alertgreen}[1]{ % citing in a slide
    \textcolor{green!50!black}{#1}
    }

\newcommand{\alertblue}[1]{ % citing in a slide
    \textcolor{blue!85!yellow}{#1}
    }

\newcommand{\citing}[1]{ % citing in a slide
    {\footnotesize \rm \emph{\textcolor{gray!85!blue}{#1.}}}
}

\newcommand{\PT}[1]{
	\mathbf{#1}
}

\newcommand{\bigo}[2][]{ % big O
	\mathcal{O}_{#1}\left(#2\right)
}

\newcommand{\Rd}[1][d]{ % d-dimensional real Euclidean space
    \mathbb{R}^{#1}
}

\newcommand{\Cd}[1][d]{ % d-dimensional real Euclidean space
    \mathbb{C}^{#1}
}

\newcommand{\Rplus}{ % non-negative real numbers
    \mathbb{R}_{+}
}

\newcommand{\Z}{ % integers ..., -1, 0, 1, ...
    \mathbb{Z}
}

\newcommand{\Zd}[1][d]{ % integer lattice points
    \mathbb{Z}^{#1}
}

\newcommand{\Zp}{ % positive integers 1,2,...
    \mathbb{Z}_{+}
}

\newcommand{\N}{ % natural numbers 1,2,...
    \mathbb{N}
}

\newcommand{\Nz}{ % natural numbers 0,1,2,...
    \mathbb{N}_{0}
}

\newcommand{\imu}{ % imagenary unit
    i
}

\newcommand{\LBo}[1][]{ %Laplace-Beltrami operator
	\Delta^{*}_{#1} 
}

\newcommand{\LBO}[1]{\left(\Delta^{*}\right)^{#1}
}

\newcommand{\sfgrad}[1][]{ % surface-gradient
	\nabla_{*}
}

\newcommand{\sfcurl}[1][]{ % surface-gradient
	\mathbf{L}
}

\newcommand{\Beo}[1]{ %Bessel operator
{\mathbf{J}_{#1}}
}

\newcommand{\id}{ %identity operator
    \mathrm{Id}
}

\newcommand{\eigvm}[1][\ell]{ % eigenvalues of Laplace-Beltrami operator on manifold
    \lambda_{#1}
}

\newcommand{\eigvs}[1][\ell,m]{ % eigenvalues of Laplace-Beltrami operator on S^d
    \eigvm[#1]
}

\newcommand{\eigfm}[1][\ell]{ % eigenvalues of Laplace-Beltrami operator on manifold
    u_{#1}
}

\newcommand{\Jcb}[2][\alpha,\beta]{
    P^{(#1)}_{#2}
}

\newcommand{\Gegen}[2][\ell]{   % Gegenbauer polynomial
{C}^{(#2)}_{#1}
}

\newcommand{\NGegen}[2][d+1]{   % normalised Gegenbauer polynomial
  P^{(#1)}_{#2}
}

\newcommand{\aLegen}[2][\ell]{   % associated Legendre polynomial
{P}^{(#2)}_{#1}
}

\newcommand{\Legen}[1][\ell]{   % associated Legendre polynomial
{P}_{#1}
}

\newcommand{\dist}[1] { % distance function
  \rho(#1)}

\newcommand{\scap}[1] { % spherical cap C(x,\delta)
  \mathcal{C}\left(#1\right)
  }

\newcommand{\sph}[1]{ % sphere S^d
    \mathbb{S}^{#1}
}

\newcommand{\Lp}[3][]{ % Lp-functions space on the sphere L_p(S^d) or L_p(S^d,\sigma_d)
    L_{#2}(\sph{#3}#1)
}

\newcommand{\vLp}[3][]{ % vector Lp-functions space on the sphere
    \mathbf{L}_{#2}(\sph{#3}#1)
}

\newcommand{\sob}[3]{ % Sobolev space on the sphere S^d
\mathbb{W}_{#1}^{#2}(\sph{#3})}

\newcommand{\ellp}[2][\Z]{ % Lp-functions space on the sphere L_p(S^d) or L_p(S^d,\sigma_d)
\ell_{#2}(#1)
}

\newcommand{\Fcoe}[2][\ell,m]{ % Fourier coefficients for f on S^d
    \widehat{#2}_{#1}
}

\newcommand{\flm}[1][\ell,m]{ % Fourier coefficients for scalar spherical harmonics on S^d
    {\rm F}_{#1}
}

\newcommand{\dflm}[1][\ell,m]{ % divergence-free Fourier coefficients for on S^d
    \widehat{\mathbf{F}}_{#1}
}

\newcommand{\cflm}[1][\ell,m]{ % curl-free Fourier coefficients for on S^d
    \widetilde{\mathbf{F}}_{#1}
}

\newcommand{\dfco}[2][\ell,m]{ % div-free Fourier coefficients on S^d
    \widehat{#2}_{#1}
}

\newcommand{\cfco}[2][\ell,m]{ % curl-free Fourier coefficients for f on S^d
    \widetilde{#2}_{#1}
}

\newcommand{\FT}[1]{ % Fourier transform
    \widehat{#1}
}

\newcommand{\FS}[1]{ % Fourier series
    \widehat{#1}
}

\newcommand{\shY}[1][\ell,m]{ % spherical harmonics
	Y_{#1}
}

\newcommand{\dsh}[1][\ell,m]{ % div-free vector spherical harmonics
	\mathbf{y}_{#1}
}

\newcommand{\csh}[1][\ell,m]{ % curl-free vector spherical harmonics
	\mathbf{z}_{#1}
}

\newcommand{\polysph}[1][]{ % spherical polynomial p or p_{\ell}
  p_{#1}
}

\newcommand{\polyspm}[1][n]{ % diffusion polynomial space on a manifold
  \Pi_{#1}
}

\newcommand{\spann}{ % span
  \mathrm{span}
}

\newcommand{\norm}[2]{ % norm \| \|_{}
\|#1\|_{#2}}

\newcommand{\InnerL}[2][]{ % inner product on a manifold
\left\langle #2 \right\rangle_{#1}
}


%% Differential element for integrals
\newcommand{\ds}[1][\bx]{    % differental element on S^d
\mathrm{d}\sigma(#1)
}

\newcommand{\fav}{{\textbf{FaVeST}}}

\newcommand{\IntDiff}[2][]{    % differental element on S^d
\:\mathrm{d}\sphm[#1](\PT{#2})
}

\newcommand{\dt}[1][t]{ % differential element on R
\mathrm{d}{#1}
}

\newcommand{\IntD}[1]{ % differential element on R
\:\mathrm{d}{#1}
}

\newcommand{\supp}{ % support of a function
\mathrm{supp}\:
}

%% the following are the symbols for random maths

\DeclareMathOperator{\PP}{\mathrm{P}}

\newcommand{\probSp}{ % probability space
 \Omega
}

\newcommand{\probB}{ % probability space
 \mathcal{F}
}

\newcommand{\probm}{ % probability measure
 \PP
}

\newcommand{\prob}[1]{ % probability measure
 \mathrm{Prob}\left(#1\right)
}

\newcommand{\Lpprob}[2][]{ % Lp space on probability space
    \mathbb{L}_{#2}\left(\probSp#1\right)
}

\newcommand{\psSp}[1][d]{ % product space of probability space and sphere
 \probSp\times \sph{#1}
}

\newcommand{\psm}[1][d]{ % product space of probability space and sphere
 \probm \otimes \sphm[#1]
}

\newcommand{\prodpsph}[1][d]{ % product space of probability space and sphere
 \probSp\times \sph{#1}
}

\newcommand{\prodpsphm}[1][d]{ % product space of probability space and sphere
 \probm \otimes \sphm[#1]
}

\newcommand{\Lppsph}[3][]{ % product space of probability space and sphere
 L_{#2}\left(\prodpsph[#3]#1\right)
}

\newcommand{\aeP}[1][\probm]{ % almost everywhere
\mathrm{a.e.}\; {#1}
}

\newcommand{\Pas}[1][\probm]{ % almost everywhere
{#1\hspace{-0.5mm}-}\mathrm{a.s.}
}

\newcommand{\expect}[1]{ % the expectation of a random variable
\mathbb{E}\left[#1\right]
}

\newcommand{\vecone}{ % vector of 1
    \boldsymbol{1}
}

\newcommand{\var}[1]{ % the variance of a random variable
\mathrm{Var}\left[#1\right]
}

\newcommand{\RF}{ % random field
    \boldsymbol{Z}
}

\newcommand{\floor}[1]{ % the expectation of a random variable
\left\lfloor #1 \right\rfloor
}

\newcommand{\ceil}[2][]{ % the expectation of a random variable
\lceil #2\rceil_{#1}
}

\newcommand{\QN}[1][N]{ % quadrature rule Q_{N_j}
    \mathcal{Q}_{#1}
}

%%%%%
%\newcommand{\C}{\mathbb{C}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\fft}[1][j]{\mathbf{F}_{#1}}
\newcommand{\Id}{\mathbf{I}}
\newcommand{\setsep}{:\hspace{0.5mm}}
\newcommand{\bbS}{{\bf S}}
\newcommand{\bx}{{\bf x}}

%%%%%%

\endinput
