dnl Process this file with autoconf to produce a configure script.

AC_INIT(Makefile.in)

AC_CHECK_PROGS(AUTOCONF,	autoconf)
AC_CHECK_PROGS(DEROFF,		[deroff cat])
AC_CHECK_PROGS(DISTILL,		[ps2pdf distill])
AC_CHECK_PROGS(MAN2HTML,	man2html)

AC_OUTPUT(Makefile)
