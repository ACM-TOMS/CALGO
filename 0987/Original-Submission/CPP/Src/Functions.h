#include <iostream>//Library for input and output data.
#include <cmath>//Library for mathematical functions as abs(x), sqrt(x), sin(x) etc.

//Prototypes of user's functions
long double example1(long double x);
long double example2(long double x);
long double fun100(long double x);
long double fun1000(long double x);
long double example(long double x);
long double fun(long double x);

