#!/usr/local/bin/perl
use strict;
use warnings;

open(OLD,"<$ARGV[0]");
my @text = <OLD>;
close OLD;

while($text[$#text] =~ /^\s*$/){
   $#text--;
}
chomp $text[$#text];
$text[$#text] .= "\n";

open (NEW, ">New/$ARGV[0]");
print NEW @text;
close NEW
