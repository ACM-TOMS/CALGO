# Fortran compiler
FC = pgfortran

# Fortran compiler flags. -Kieee is required to disable flush to zero
FFLAGS = -O3 -Kieee

# Preprocessor flags
CPPFLAGS = -DUSE_IEEE_INTRINSIC

# Specify the location of compiled modules
MODOPT = -module
