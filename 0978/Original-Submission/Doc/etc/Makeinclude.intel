# Fortran compiler
FC = ifort

# Fortran compiler flags
FFLAGS = -O3 -fp-model precise -u -warn unused -xHost

# Preprocessor flags
CPPFLAGS = -DUSE_IEEE_INTRINSIC

# Specify the location of compiled modules
MODOPT = -module
