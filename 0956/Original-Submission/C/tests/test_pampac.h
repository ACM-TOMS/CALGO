#include "pampac.h"

extern options_struct set_options(int, int);
extern void free_options (options_struct*);
extern PTnode* make_tree(options_struct *opts);
extern void tree43 (PTnode*, options_struct*);

